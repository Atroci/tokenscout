node_modules/
dist/
*.log
.DS_Store

# Cloned upstream projects to study / prepare PRs against — never committed here.
_upstreams/*
!_upstreams/README.md

# Ad-hoc dogfooding scans against real sites — local scratch, not published.
/findings/
/scripts/
